# The Round Up

## Python Environment

This project uses a backend-scoped virtual environment at `backend/.venv`.

- From repo root: `source backend/.venv/bin/activate`
- Or from `backend/`: `source .venv/bin/activate`

If imports (like `fastapi`) are unresolved in the editor, make sure the interpreter is set to `backend/.venv/bin/python`.
